<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:clipChildren="false">

    <!-- Container for both the panel and the AI button to keep them together -->
    <LinearLayout
        android:layout_width="72dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="12dp"
        android:clipChildren="false"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <!-- Main Sidebar Pill -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/panelCard"
            android:layout_width="72dp"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_panel"
            android:elevation="32dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <!-- Scrollable app list (Strictly 300dp to fit exactly 5 items) -->
            <!-- Using Fixed Height guarantees the 5-app rule -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvPanelApps"
                android:layout_width="match_parent"
                android:layout_height="320dp"
                android:clipToPadding="false"
                android:overScrollMode="ifContentScrolls"
                android:paddingBottom="4dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toTopOf="@id/btnClose" />

            <!-- Chevron at bottom of pill -->
            <ImageButton
                android:id="@+id/btnClose"
                android:layout_width="32dp"
                android:layout_height="24dp"
                android:layout_marginTop="4dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Close panel"
                android:src="@drawable/ic_chevron_right"
                android:tint="#80FFFFFF"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@id/rvPanelApps" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Floating AI Button (BlueLM Style) - Now pinned right below the panel -->
        <LinearLayout
            android:id="@+id/btnAI"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/bg_ai_button"
            android:elevation="40dp"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:text="AI"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold" />
            
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="BlueLM"
                android:textColor="#B3FFFFFF"
                android:textSize="7sp" />
        </LinearLayout>

    </LinearLayout>

</RelativeLayout>